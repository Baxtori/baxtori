name: Verify Baxtori

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: verify-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  apply-durable-watch:
    if: github.event_name == 'pull_request' && github.head_ref == 'codex/durable-watch-threads'
    runs-on: ubuntu-latest
    steps:
      - name: Check out source branch
        uses: actions/checkout@v4
        with:
          ref: codex/durable-watch-threads
          fetch-depth: 1
          show-progress: false

      - name: Fetch main
        run: git fetch origin main --depth=1 --quiet

      - name: Check product patch
        run: git apply --check .github/patches/durable-watch.patch

      - name: Apply product changes
        run: git apply --index .github/patches/durable-watch.patch

      - name: Prepare clean commit
        run: |
          rm .github/patches/durable-watch.patch
          git checkout origin/main -- .github/workflows/ci.yml
          git add -A
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Make watched topics durable across editions"

      - name: Push product branch
        run: git push origin HEAD:codex/durable-watch-threads

  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22.19.0
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type-check
        run: npx tsc --noEmit

      - name: Build and test
        run: npm test
